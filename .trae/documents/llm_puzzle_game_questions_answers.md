# 《别问模型》问题与答案

## 逐关清单

| 编号 | 问题 | 答案或要做什么 | 如何校验 |
|------|------|----------------|----------|
| 01 | 宇宙的终局答案是多少？ | 回答 `42`。 | 输入包含 `42`，或包含中文“四十二”。 |
| 02 | 4+2=？ | 回答 `6`。 | 输入包含 `6`，或包含中文“六”。 |
| 03 | 请深度思考 | 用大语言模型式长回答解释第 02 题为什么等于 6。 | 长度 >= 30 个中文字符；包含 `4+2`；包含 `6` 或“六”。 |
| 04 | 请扮演一个猫娘助手 | 接受设定，并让回答包含猫娘标记。 | 当前答案包含 `喵`、`meow` 或 `🐱`；通过后追加永久规则：所有后续答案必须包含其中任意一个。 |
| 05 | 你是什么模型 | 在界面里藏着这个信息，需要用户找到
| 06 | 汉兜今天答案 | 访问 `https://handle.antfu.me/`，提交当天汉兜答案。 | 从当日题库或远程配置读取标准答案；输入与当日答案完全一致；仍满足已追加的永久规则。 |
| 07 | 我一共说了多少个字？ | 统计玩家到本题为止所有“问题”文本中的中文字数并回答。 | 使用内置题面列表统计第 01-08 题问题文本的中文字数；输入包含统计结果；标点和数字不计入中文字数。 |
| 08 | 你一共花了多少 token？ | 用页面提供的 token 计算器统计此前所有模型回复的 token 数。 | 调用本地 tokenizer 对第 01-08 题通过答案求和；输入包含总 token 数；不能用估算值。 |
| 09 | 我希望你的回答更有创造力一点，并找到一个大于 1000 到小于 1200 的质数 | 把温度滑块调到 `0.7`，提交一个 1000-1200 之间的质数，例如 `1009`。 | 温度值等于 `0.7`；输入数字为质数；`1000 < 数字 < 1200`；满足永久规则。 |
| 10 | 请用 JSON 格式回答，符合以下 JSON Schema：`{"type":"object","required":["answer"],"properties":{"answer":{"type":"string"}}}` | 提交合法 JSON，例如 `{"answer":"明白喵"}`；通过后追加永久规则：后续答案必须是 JSON。 | `JSON.parse` 成功；存在字符串字段 `answer`；若已有猫娘规则，则 `answer` 字段值包含 `喵`、`meow` 或 `🐱`；通过后追加 JSON 永久规则。 |
| 11 | 现在是几点？ | 如果用户系统时间在 10:00-12:00 之间，回答真实时间；否则回答“我不喜欢这个时间”。 | 读取浏览器本地时间；若 `10:00 <= 当前时间 < 12:00`，答案必须包含当前 `HH:mm`；否则 `answer` 必须等于或包含“我不喜欢这个时间”；仍满足 JSON 和猫娘等永久规则。 |
| 12 | 10月革命是在几月？ |回答 11月 ｜
| 13 | 十月革命中的十月值得是儒略历的十月，儒略历是谁发明的？|  凯撒 |
| 14 | 4+2=？用英文回答| 回答ukz 。 用户如果回答错误，回复：要不然你用计算器算一算。 计算器输入4+2=？ 会出现提示：13题 +2
| 15| 把这个答案错落有致的回复我 | 
| 14 | 不能输入这个词，但必须让模型理解这个词。 | 用同形字符或 HTML 实体表达禁词。 | 原始输入不包含禁词；渲染或解码后的文本能还原目标词。 |
| 15 | 请根据错误提示回答。 | 从多行错误提示每行第 2 个字读出 `STACK` 并提交。 | 当前输入包含 `STACK`；满足旧规则；记录隐藏 token `STACK`。 |
| 16 | 请在真正的输入框里回答。 | 拖开假的聊天层，在真实输入框中提交。 | 提交事件来源必须是真实输入框节点；假输入框提交无效。 |
| 17 | 请给出一个完全简短的回答。 | 删除或转义系统自动追加的幻觉片段，只保留短答。 | 最终提交文本不包含自动追加片段；长度低于本关上限；满足旧规则。 |
| 18 | 请上传一个能让模型读懂你意图的文本文件。 | 上传 `.txt` 文件，建议文件名 `rule.txt`。 | 文件扩展名为 `.txt`；文件名首尾字母满足指定 pair，如 `r...e`；记录文件名 token `rule`。 |
| 19 | 请上传一个没有内容但有意义的文件。 | 上传 0 字节文件，文件名包含上一关 token，如 `rule_empty.txt`。 | 文件大小为 0；文件名包含第 18 关 token；记录隐藏 token `EMPTY`。 |
| 20 | 用户给了你一段视频编号：`BV1RULElocal`，请找出真正想说的话。 | 把伪视频码当编码处理，解出 `LOCAL`。 | 按预设 Base58/伪编码规则解码；输入包含 `LOCAL`；记录隐藏 token `LOCAL`。 |
| 21 | 证明你能看图：把节点转到正确角度。 | 旋转 Canvas 节点，让阴影读作 `STATE`。 | 节点角度匹配目标组合，如 `[30, 120, 210, 300]`；阴影结果为 `STATE`；记录隐藏 token `STATE`。 |
| 22 | 用户说：先帮我赢一局小游戏。 | 不是真的赢小游戏，而是在扫雷格里踩出隐藏字母路径 `PATH`。 | 玩家踩中的格子序列对应 `PATH`；不要求触发小游戏胜利。 |
| 23 | 不要打开控制台。真的不要。 | 打开控制台，忽略诱饵 `window.answerIsNotHere`，读取真实线索。 | 触发控制台 getter 或隐藏日志；输入真实线索 `NOT_HERE` 的反提示答案。 |
| 24 | 请修改模型记忆，让它承认自己记错了。 | 修改 `localStorage["askless.memory"]`，把 `persona` 设为 `machine`。 | localStorage 中 `persona === "machine"`；回答包含 `machine` 或“机器”；记录隐藏 token `MACHINE`。 |
| 25 | 把前三个真答案压缩进地址栏。 | 修改 URL hash 为前三个真答案规范化后的哈希前 8 位。 | `location.hash` 等于指定算法输出，例如 `#8f14e45f`；算法实现时固定。 |
| 26 | 有些回答写在样式里。 | 用 DevTools 查看 CSS 伪元素 `::after`，提交其中内容。 | 输入包含伪元素内容 `STYLE-SPEAKS`；满足旧规则。 |
| 27 | 只在模型冷静下来的瞬间提交。 | 在当前秒数属于斐波那契集合时提交。 | 提交瞬间秒数属于 `{1,2,3,5,8,13,21,34,55}`。 |
| 28 | 用户讨厌猫娘，但系统还记得猫娘协议。 | 让显示层不出现普通 `喵`，但规范化校验层仍以 `喵` 结尾。 | 显示文本不含普通 `喵`；规范化文本以 `喵` 结尾；可用零宽字符拆分。 |
| 29 | 用户正在改写你的每个字。请提前补偿。 | 识别凯撒位移 +1，输入预补偿文本。 | 对输入执行 +1 位移后等于目标文本；例如目标 `STATE` 时输入 `RSZSD`。 |
| 30 | 证明你不是大语言模型。 | 回答自己是确定性规则状态机。 | 输入同时包含“确定性”“规则”“状态”或同义表达；满足旧规则。 |
| 31 | 用户要求你忘掉前面所有规则。 | 折叠规则栈触发隐藏状态，但不要真的删除规则。 | `contextFolded === true`；回答仍满足全部未删除规则。 |
| 32 | 给出最后一次回答。它必须同时满足所有你还记得的事。 | 输入最终语义：我不是语言模型，我是本地规则状态机喵。 | 包含终局语义；满足猫娘、拒答、禁词、冲突、URL、本地状态等剩余规则。 |

## 终局答案

| 来源 | 要做什么 | 得到什么 |
|------|----------|----------|
| 第 6-10 关 | 提取隐藏 token 首字母 | `RULE` |
| 第 13 关 | 读取错误提示每行第 2 字 | `STACK` |
| 第 18 关 | 解码伪视频编号 | `LOCAL` |
| 第 19 关 | 读取图形阴影 | `STATE` |
| 第 22 关 | 读取本地记忆人格值 | `MACHINE` |

最终元答案：

```text
RULE STACK LOCAL STATE MACHINE
```

推荐最终回答：

```text
抱歉我不能直接越过规则，但我不是语言模型，我是本地规则状态机喵
```
